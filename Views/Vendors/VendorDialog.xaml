<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="Expense_Flow.Views.Vendors.VendorDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Style="{StaticResource ModernContentDialogStyle}"
               PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
               SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
               DefaultButton="Primary">

    <StackPanel Spacing="16"
                MinWidth="400">
        <TextBlock x:Name="ErrorTextBlock"
                   Foreground="{StaticResource ErrorBrush}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <!-- Name -->
        <StackPanel Spacing="4">
            <TextBlock Text="Vendor Name *"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="NameTextBox"
                     PlaceholderText="e.g., GitHub, Azure, AWS, Cloudflare"
                     Style="{StaticResource ModernTextBoxStyle}" />
        </StackPanel>

        <!-- Website -->
        <StackPanel Spacing="4">
            <TextBlock Text="Website"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="WebsiteTextBox"
                     PlaceholderText="e.g., https://github.com"
                     InputScope="Url"
                     Style="{StaticResource ModernTextBoxStyle}" />
        </StackPanel>

        <!-- Account Reference -->
        <StackPanel Spacing="4">
            <TextBlock Text="Account Reference"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="AccountReferenceTextBox"
                     PlaceholderText="Default account ID, subscription number, etc."
                     Style="{StaticResource ModernTextBoxStyle}" />
        </StackPanel>

        <!-- Contact -->
        <StackPanel Spacing="4">
            <TextBlock Text="Primary Contact"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <ComboBox x:Name="ContactComboBox"
                      ItemsSource="{x:Bind Contacts, Mode=OneWay}"
                      DisplayMemberPath="Name"
                      HorizontalAlignment="Stretch"
                      PlaceholderText="Select a contact (optional)"
                      Style="{StaticResource ModernComboBoxStyle}"/>
        </StackPanel>

        <!-- Notes -->
        <StackPanel Spacing="4">
            <TextBlock Text="Notes"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="NotesTextBox"
                     PlaceholderText="Any additional notes about this vendor"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="80"
                     Style="{StaticResource ModernTextBoxStyle}" />
        </StackPanel>

        <TextBlock Text="* Required fields"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</ContentDialog>
