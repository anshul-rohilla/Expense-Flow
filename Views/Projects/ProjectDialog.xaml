<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="Expense_Flow.Views.Projects.ProjectDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Style="{StaticResource ModernContentDialogStyle}"
               PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
               SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
               DefaultButton="Primary">

    <ScrollViewer MaxHeight="600">
        <StackPanel Spacing="16"
                    MinWidth="400">
            <TextBlock x:Name="ErrorTextBlock"
                       Foreground="{StaticResource ErrorBrush}"
                       TextWrapping="Wrap"
                       Visibility="Collapsed" />

            <StackPanel Spacing="4">
                <TextBlock Text="Name *"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox x:Name="NameTextBox"
                         PlaceholderText="Enter project name"
                         Style="{StaticResource ModernTextBoxStyle}" />
            </StackPanel>

            <StackPanel Spacing="4">
                <TextBlock Text="Description *"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox x:Name="DescriptionTextBox"
                         PlaceholderText="Enter project description (required)"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         MinHeight="80"
                         Style="{StaticResource ModernTextBoxStyle}" />
            </StackPanel>

            <StackPanel Spacing="4">
                <TextBlock Text="Monthly Budget"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <NumberBox x:Name="BudgetNumberBox"
                          PlaceholderText="Enter monthly budget amount"
                          SpinButtonPlacementMode="Inline"
                          Minimum="0"
                          SmallChange="100"
                          LargeChange="1000"
                          CornerRadius="8"
                          MinHeight="44"/>
                <TextBlock x:Name="AnnualBudgetHint"
                          Style="{StaticResource CaptionTextBlockStyle}"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                          Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Spacing="4">
                <TextBlock Text="Default Payment Mode"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <ComboBox x:Name="PaymentModeComboBox"
                          ItemsSource="{x:Bind PaymentModes, Mode=OneWay}"
                          DisplayMemberPath="Name"
                          HorizontalAlignment="Stretch"
                          Style="{StaticResource ModernComboBoxStyle}" />
            </StackPanel>

            <StackPanel Spacing="4">
                <TextBlock Text="Project Group"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <ComboBox x:Name="ProjectGroupComboBox"
                          ItemsSource="{x:Bind ProjectGroups, Mode=OneWay}"
                          DisplayMemberPath="Name"
                          HorizontalAlignment="Stretch"
                          PlaceholderText="Select group (optional)"
                          Style="{StaticResource ModernComboBoxStyle}" />
                <TextBlock Text="Assign this project to a group for better organization"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           FontSize="11" />
            </StackPanel>

            <TextBlock Text="* Required fields"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>