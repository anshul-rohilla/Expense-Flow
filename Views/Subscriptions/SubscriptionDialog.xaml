<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="Expense_Flow.Views.Subscriptions.SubscriptionDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Style="{StaticResource ModernContentDialogStyle}"
               PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
               SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
               DefaultButton="Primary">

    <StackPanel Spacing="16"
                MinWidth="400">
        <TextBlock x:Name="ErrorTextBlock"
                   Foreground="{StaticResource ErrorBrush}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <StackPanel Spacing="4">
            <TextBlock Text="Type *"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" Spacing="12">
                <RadioButton x:Name="SubscriptionRadioButton"
                             Content="Subscription"
                             IsChecked="True"
                             GroupName="EntityType"/>
                <RadioButton x:Name="VendorRadioButton"
                             Content="Vendor"
                             GroupName="EntityType"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Name *"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="NameTextBox"
                     PlaceholderText="Enter subscription/vendor name (e.g., Netflix, Amazon, Supplier Ltd)"
                     Style="{StaticResource ModernTextBoxStyle}" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Category *"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <ComboBox x:Name="TypeComboBox"
                      ItemsSource="{x:Bind ExpenseTypes, Mode=OneWay}"
                      DisplayMemberPath="Name"
                      HorizontalAlignment="Stretch"
                      PlaceholderText="Select category (e.g., OTT &amp; Streaming, IT &amp; Software, Shopping)"
                      Style="{StaticResource ModernComboBoxStyle}"/>
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Reference"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <TextBox x:Name="ReferenceTextBox"
                     PlaceholderText="Account ID, email, or vendor code (optional)"
                     Style="{StaticResource ModernTextBoxStyle}" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock Text="Contact *"
                       Style="{StaticResource BodyStrongTextBlockStyle}" />
            <ComboBox x:Name="ContactComboBox"
                      ItemsSource="{x:Bind Contacts, Mode=OneWay}"
                      DisplayMemberPath="Name"
                      HorizontalAlignment="Stretch"
                      Style="{StaticResource ModernComboBoxStyle}" />
        </StackPanel>

        <TextBlock Text="* Required fields"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</ContentDialog>
