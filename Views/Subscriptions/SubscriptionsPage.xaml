<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Expense_Flow.Views.Subscriptions.SubscriptionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Expense_Flow.Models"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <!-- Compact -->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Padding" Value="16"/>
                    </VisualState.Setters>
                </VisualState>
                
                <!-- Medium -->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Padding" Value="24"/>
                    </VisualState.Setters>
                </VisualState>
                
                <!-- Large -->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1007" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Padding" Value="32,24,32,32"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="Subscriptions" 
                         Style="{StaticResource PageTitleTextStyle}"/>
                <TextBlock Text="Track where your payments originate from"
                         Style="{StaticResource BodyTextBlockStyle}"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                         TextWrapping="Wrap"/>
            </StackPanel>

            <Button Grid.Column="1"
                  Content="Add Subscription" 
                  Style="{StaticResource PrimaryButtonStyle}"
                  VerticalAlignment="Center"
                  Click="AddSubscription_Click">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="N" Modifiers="Control"/>
                </Button.KeyboardAccelerators>
            </Button>
        </Grid>

        <!-- Search Bar with Acrylic Background -->
        <Border Grid.Row="1"
              Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
              CornerRadius="12"
              Padding="16"
              Margin="0,0,0,24"
              Translation="0,0,8">
            <TextBox PlaceholderText="Search Subscriptions..."
                   Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   BorderThickness="0"
                   Background="Transparent">
                <TextBox.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F" Modifiers="Control"/>
                </TextBox.KeyboardAccelerators>
            </TextBox>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="2">
            <!-- Loading Indicator -->
            <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                        Width="50" Height="50"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <!-- Error Message -->
            <InfoBar Severity="Error"
                   IsOpen="{x:Bind ViewModel.HasErrors, Mode=OneWay}"
                   Title="Error"
                   Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                   Margin="0,0,0,16"
                   VerticalAlignment="Top"/>

            <!-- Empty State -->
            <Border Style="{StaticResource StatCardStyle}"
                  Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
                  Translation="0,0,16"
                  Visibility="{x:Bind ViewModel.Subscriptions.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                <StackPanel Spacing="20" Padding="48">
                    <Border Background="{StaticResource SuccessGradientBrush}"
                          Width="80" Height="80"
                          CornerRadius="40"
                          HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE910;" FontSize="40" Foreground="White"/>
                    </Border>
                    <TextBlock Text="No Subscriptions yet"
                             Style="{StaticResource SectionTitleTextStyle}"
                             HorizontalAlignment="Center"/>
                    <TextBlock Text="Add Subscriptions to track where payments originate from"
                             Style="{StaticResource BodyTextBlockStyle}"
                             Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                             HorizontalAlignment="Center"
                             TextWrapping="Wrap"
                             TextAlignment="Center"
                             MaxWidth="400"/>
                    <Button Content="Add Your First Subscription"
                          Style="{StaticResource PrimaryButtonStyle}"
                          HorizontalAlignment="Center"
                          Click="AddSubscription_Click"/>
                </StackPanel>
            </Border>

            <!-- Subscriptions List -->
            <ScrollViewer Visibility="{x:Bind ViewModel.Subscriptions.Count, Mode=OneWay, Converter={StaticResource InverseCountToVisibilityConverter}}">
                <ItemsRepeater ItemsSource="{x:Bind ViewModel.Subscriptions, Mode=OneWay}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="16"/>
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="models:Subscription">
                            <Border Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
                                  Style="{StaticResource CardStyle}"
                                  Translation="0,0,8">
                                <Grid ColumnSpacing="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Gradient Icon -->
                                    <Border Grid.Column="0"
                                          Background="{StaticResource SuccessGradientBrush}"
                                          Width="56" Height="56"
                                          CornerRadius="16">
                                        <FontIcon Glyph="&#xE910;" Foreground="White" FontSize="28"/>
                                    </Border>

                                    <!-- Subscription Info -->
                                    <StackPanel Grid.Column="1" Spacing="6" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Name}" 
                                                 Style="{StaticResource CardTitleTextStyle}"/>
                                        <StackPanel Orientation="Horizontal" Spacing="16">
                                            <StackPanel Orientation="Horizontal" Spacing="6"
                                                      Visibility="{x:Bind Type, Converter={StaticResource NullToVisibilityConverter}}">
                                                <FontIcon Glyph="&#xE8EA;" FontSize="14" Foreground="{StaticResource AccentBrush}"/>
                                                <TextBlock Text="{x:Bind Type}" 
                                                         Style="{StaticResource CaptionTextBlockStyle}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="6"
                                                      Visibility="{x:Bind Reference, Converter={StaticResource NullToVisibilityConverter}}">
                                                <FontIcon Glyph="&#xE77B;" FontSize="14" Foreground="{StaticResource PrimaryBrush}"/>
                                                <TextBlock Text="{x:Bind Reference}" 
                                                         Style="{StaticResource CaptionTextBlockStyle}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Column="2" 
                                              Orientation="Horizontal" 
                                              Spacing="8"
                                              VerticalAlignment="Center">
                                        <Button Style="{StaticResource IconButtonStyle}"
                                              Click="EditSubscription_Click"
                                              Tag="{x:Bind}"
                                              ToolTipService.ToolTip="Edit">
                                            <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                                        </Button>
                                        <Button Style="{StaticResource IconButtonStyle}"
                                              Click="DeleteSubscription_Click"
                                              Tag="{x:Bind}"
                                              ToolTipService.ToolTip="Delete">
                                            <FontIcon Glyph="&#xE74D;" FontSize="16" Foreground="{StaticResource ErrorBrush}"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
